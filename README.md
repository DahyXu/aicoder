# aicoder

all code is designed by AI。

## TypeScript

This project is now configured to use TypeScript.

### Commands

- `npm run typecheck`: Type-check the project.
- `npm run build`: Compile TypeScript into `dist/`.
- `npm run start`: Run the compiled output.
